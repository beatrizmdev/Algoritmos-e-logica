Algoritmo "operadoresLogicosCombinados"

Var

Inicio

      // ( (V) e (V) ) ou ( (F) e (V) )
      // (V) ou (F)
      // (V)

      se ((7 > 4) e ("a" <> "b")) ou ( nao(5 = 5) e ( (10+9) >= 9) ) então
         escreva("Se verdadeiro")
      senao
         escreva("Se falso")
      fimse

Fimalgoritmo